# Used by `npm run dev:localfunc` / `npm run build:localfunc`: points the
# extension straight at a locally running Azure Functions host (func start,
# default port 7071) instead of the Angular proxy on 4200 or the deployed
# Static Web App.
WXT_API_BASE_URL=http://localhost:7071
WXT_CONFIG_URL=http://localhost:7071
